# --- apps/web ---
NEXT_PUBLIC_API_URL=http://localhost:3001

# Geoapify map tiles/styles key (myprojects.geoapify.com). Browser keys are
# public by design - restrict allowed origins in the Geoapify dashboard
# instead of hiding the key.
NEXT_PUBLIC_GEOAPIFY_API_KEY=
